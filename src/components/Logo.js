import logo from '../assets/images/logo.svg'

const Logo = () => {
    return (
        <div>
            <img src={logo} alt='job hunter' className='logo' />
        </div>
    )
}

export default Logo
